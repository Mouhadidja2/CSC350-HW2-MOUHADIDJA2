# CSC350 Homework 2 — Independent Remote Workflow

This is a separate homework repository. It is not the pair repository used for classroom Activities 03–08.

## Your mission

Create a clear visitor guide for a fictional festival while demonstrating the complete workflow:

```text
edit → inspect → stage → verify → commit → push → remote edit → pull → verify
```

Begin with `STUDENT-INSTRUCTIONS.md`. Do not run `git init` because this folder is already a Git repository.

